naming conventions changed 
access of secret key and expiry via env file
unit testing for user,chat,auth -- services and resolver done
Core business logic --✅ UsersService,✅ ChatService
GraphQL layer --✅ UsersResolver,✅ ChatResolver
Authentication internals --✅ GqlAuthGuard,✅ JwtStrategy

Existing logic  to firebase

POST login(email, password)
→ backend verifies password
→ backend signs JWT using JWT_SECRET
→ returns accessToken (your JWT)
→ guards verify JWT_SECRET


POST login(email, password)
→ backend authenticates user with Firebase
→ Firebase issues ID token (signed by Google)
→ backend bumps tokenVersion + sets Firebase custom claim
→ backend returns accessToken = Firebase ID token
→ guards verify token using firebase-admin

Added update query to change firstname,lastname and mobilenumber
Added update query to change password and also old password is not working
